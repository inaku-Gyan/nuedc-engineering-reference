# Agent Instructions

本仓库是供 Agent 按需查阅和维护的 NUEDC 工程知识库。所有任务必须先完成
“模式加载”，再按模式允许的范围检索、整理或获取资料。

## 0. 必须先加载 Agent 模式

本地模式文件为仓库根目录的 `.agent-mode.yaml`，完整规则见
[`docs/agent-modes.md`](docs/agent-modes.md)。

开始任何实际任务前必须：

1. 检查 `.agent-mode.yaml` 是否存在。
2. 存在时完整读取并校验 `schema_version`、`mode` 和 `git`。
3. 配置有效后，明确当前模式以及是否启用自动提交/推送。
4. 只在当前模式或用户明确授予的临时权限范围内继续任务。

### 配置不存在

不得执行原任务，也不得自行选择默认模式。必须暂停原任务并依次询问：

1. 模式：`readonly`、`curate-on-use`、`autonomous` 或 `maintainer`。
2. 如果所选模式允许写入，是否启用 `git.auto_commit`。
3. 仅当 `auto_commit: true` 时，是否启用 `git.auto_push`。
4. 展示完整 YAML 和副作用说明，请用户确认。

用户确认后才可创建 `.agent-mode.yaml`。创建后必须重新读取和校验，再恢复此前
暂停的任务。用户取消、拒绝配置或无法交互时，保持阻塞。

选择 `readonly` 时，`auto_commit` 和 `auto_push` 必须固定为 `false`，不再询问。

### 配置无效

配置无法解析、版本或模式未知、字段缺失/类型错误、存在未知字段，或者
`auto_push: true` 但 `auto_commit: false` 时：

- 保持原文件不变并指出具体错误；
- 保留有效选项，只逐项询问缺失或无效项；
- 展示修复后的完整 YAML；
- 用户确认后才可覆盖；
- 重新读取验证成功后再恢复原任务。

配置初始化或经确认的修复，是 `readonly` 下唯一允许的持久写入例外。

### 配置所有权与临时提升

- Agent 不得自行创建、修改或删除一份已经有效的模式文件。
- 只有初始化向导、修复向导，或用户明确点名要求修改该文件时才可写入。
- 用户明确点名具体动作时，可以临时提升本次任务的权限；提升仅限指定对象和动作。
- 临时提升不修改模式文件，也不会隐式启用自动提交或推送。
- 缺失配置时仍必须先完成初始化，不能用临时提升跳过。
- 用户指令和运行环境可以缩小权限；环境权限始终是实际能力上限。

## 1. 模式摘要

- `readonly`：可读本地资料和联网核验；禁止持久修改仓库。
- `curate-on-use`：可将本次实际使用的现有原件按需整理成摘要或摘录；禁止新增原件。
- `autonomous`：资料缺失时可围绕当前任务搜索、下载、归档和索引有用资料。
- `maintainer`：可进行主动的全库质量维护；重大删除或重组必须先询问。

模式的完整边界、价值判断、联网触发和 Git 行为以
[`docs/agent-modes.md`](docs/agent-modes.md) 为准。

## 2. 查阅顺序

1. 从 `catalog/README.md` 或相应分类索引开始。
2. 找到资料包后，先读其 `README.md` 和 `meta.yaml`。
3. 只有问题需要具体寄存器、参数、步骤或限制条件时，才读相关
   `extracts/*.md`。
4. 摘要和摘录无法回答、存在冲突或需要严格核验时，再查 `originals/`。
5. 跨器件的设计、调试与工作流问题优先查 `knowledge/`，再回到其列出的来源。

不要一次性加载整个目录、整份 PDF 或所有摘录。先通过文件名、标题、标签和
全文搜索缩小范围。

## 3. 检索与可信度

- 型号搜索同时考虑 `meta.yaml` 中的 `aliases`。
- 参数搜索优先限定到已找到的资料包。
- 使用 `rg` 搜索 Markdown/YAML；只有缺少文字化内容时才处理 PDF。
- 同名器件以厂商、完整料号和文档版本消歧。
- 官方 datasheet、reference manual、标准正文优先于博客和论坛。
- `knowledge/` 是二次知识；关键参数必须回溯到资料包或明确的外部来源。
- 回答时给出仓库相对路径；精确事实同时给出原件文档名和页码/章节。
- 若版本、适用型号或测量条件不确定，明确说明，不得自行补全。
- 摘要与原件冲突时，以适用于目标型号和版本的原件为准，并标记待修订内容。

## 4. 所有写入模式的共同规则

- 不做无目的的全量文本提取。
- 摘录必须写明来源文件、页码或章节、适用范围和提取日期。
- 摘要必须说明已覆盖和未覆盖的主题。
- 新资料按 [`docs/ingestion-guide.md`](docs/ingestion-guide.md) 归档。
- 修改原件、版本或关键结论后，同步检查资料包 `README.md`、`meta.yaml`、
  摘录和 `catalog/` 入口。
- 公开可访问的官方工程资料在许可未明确时可归档，但必须标记
  `redistribution: unknown`。
- 不得归档付费、需登录、保密、泄露或明确限制传播的资料。
- 不删除具有兼容性或历史价值的旧版本；重大删除和批量重组按模式规则处理。

