# Agent Instructions

本仓库是供 Agent 按需查阅的 NUEDC 工程知识库。检索和引用资料时遵循以下规则。

## 查阅顺序

1. 从 `catalog/README.md` 或相应分类索引开始。
2. 找到资料包后，先读其 `README.md` 和 `meta.yaml`。
3. 只有问题需要具体寄存器、参数、步骤或限制条件时，才读相关的
   `extracts/*.md`。
4. 摘要和摘录无法回答、存在冲突或需要严格核验时，再查
   `originals/` 中的原件。
5. 跨器件的设计、调试与工作流问题优先查 `knowledge/`，再回到其列出的来源。

不要一次性加载整个目录、整份 PDF 或所有摘录。先通过文件名、标题、标签和
全文搜索缩小范围。

## 检索建议

- 型号搜索同时考虑 `meta.yaml` 中的 `aliases`。
- 参数搜索优先限定到已找到的资料包。
- 使用 `rg` 搜索 Markdown/YAML；只有缺少文字化内容时才处理 PDF。
- 同名器件以厂商、完整料号和文档版本消歧。

## 可信度与引用

- 官方 datasheet、reference manual、标准正文优先于博客和论坛。
- `knowledge/` 是二次知识；关键参数必须回溯到资料包或明确的外部来源。
- 回答时给出仓库相对路径；涉及精确事实时同时给出原件文档名和页码/章节。
- 若资料版本、适用型号或测量条件不确定，明确说明，不得自行补全。
- 发现摘要与原件冲突时，以适用于目标型号和版本的原件为准，并标记待修订内容。

## 添加或修改内容

- 不做无目的的全量文本提取。
- 摘录必须写明来源文件、页码或章节、适用范围和提取日期。
- 摘要必须说明已覆盖和未覆盖的主题。
- 新资料先按 `docs/ingestion-guide.md` 归档。
- 修改原件、版本或关键结论后，同步检查资料包 `README.md`、`meta.yaml`、
  摘录和 `catalog/` 中的入口。
- 不删除旧版本原件，除非确认它重复、损坏或无保留价值；有兼容性价值的旧版本
  应明确标注。

