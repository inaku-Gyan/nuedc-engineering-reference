# Inbox

新获得但尚未完成分类、命名、许可检查或摘要的资料可临时放在这里。

`inbox/` 不是永久存储区，也不应成为 Agent 的默认知识来源。整理时需要：

1. 确认资料描述的对象、厂商、完整型号、文档类型和版本；
2. 检查是否允许在仓库中再分发；
3. 计算校验值并记录来源；
4. 移入合适的 `library/` 资料包，或只登记到外部链接索引；
5. 至少完成资料包 `README.md` 和 `meta.yaml`；
6. 有明确使用需求时再制作摘录。

除本说明外，长期不应提交未整理文件。

